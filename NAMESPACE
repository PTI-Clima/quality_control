useDynLib(qualityControl, .registration=TRUE)
importFrom(Rcpp, evalCpp)
#exportPattern("^[[:alpha:]]+")

export(launch.all.controls)
export(launch.controls)

import(chron)
import(lattice)
import(Hmisc)
import(reshape)
import(reshape2)
import(plyr)